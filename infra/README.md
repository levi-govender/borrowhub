# Bicep modules will be added in P3-03. Do not treat this folder as a complete environment.
# Planned files: main.bicep plus modules for network, registry, identity, secrets,
# monitoring, database, container-environment, apps, web, and budget.
#
# Container images (P3-02):
#   services/backend/Dockerfile  (context: services/backend)
#   services/bff/Dockerfile      (context: repository root)
#   make docker-build / make docker-up
